# Knowledge Base — registry + grounding protocol

`INDEX.md` is the agent team's KB registry: which files exist, when an
agent reads them, and the construction protocol (sources pinned, fetch
don't recall, distil + ground, cite, verify). Most topic files were
authored as `STATUS: STUB` pending first-use by specialist agents.

Snapshot 2026-05-20 — pushed per user authorisation; originals stay in
`.claude/kb/`.
