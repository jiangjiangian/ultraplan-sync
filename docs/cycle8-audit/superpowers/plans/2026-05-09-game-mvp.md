# Audit — docs/superpowers/plans/2026-05-09-game-mvp.md

**Overview (≤3):**
1. This is the Day-1 MVP plan dated 2026-05-09 (color-block placeholder game with `TrueUmbrella` only, `std::vector<unique_ptr<GameObject>>` loop, 4 GoF artefacts, 4 doctest cases). The shipped game (HEAD `b33db2b`, Cycles 1–8 converged) is a vastly evolved full RPG — 4 chapter spine, 4 umbrellas, NPCs/Vendors, SemesterStateMachine, dialogue loader, asset textures, harness, 289 tests.
2. **Every individual MVP element is satisfied or superseded by stronger shipped code** — the plan was a scaffold; CHANGELOG/BUGLEDGER track the divergence as a planned trajectory ("Day 2-3 Roadmap" §855–862 already foresees the extensions). Nothing in the plan contradicts the **current direction**.
3. All divergences are **Stale-doc-only**: C++17 → C++20 (CLAUDE.md §5 explicit), `DrawRectangle` placeholders → real textures+View, RenderRequested event-driven Draw → direct `IRenderer::Draw` (MVC re-architected), 8-line `main.cpp` stub → 120-line composition root with TitleScreen/CharacterSelect/Harness wiring. Per CLAUDE.md §2 the plan is a "design baseline & rationale, possibly stale" doc.

## Per-element annotations

- **Goal: ship MVP satisfying 7 rubric items by 2026-05-09** — single-day delivery target.
  [是否實作?] Yes — exceeded by Cycles 1–8 (CHANGELOG.md 2026-05-17 → 2026-05-20). [邏輯衝突?] No (Stale-doc-only — surpassed).
- **Tech Stack: C++17, Raylib 5.5, CMake 3.14+, doctest** — declared toolchain.
  [是否實作?] Partial — `CMakeLists.txt:4` is C++20 (lifted per CHANGELOG Cycle 1 / CLAUDE.md §5); raylib 5 (`CMakeLists.txt:33`), doctest v2.4.11 (`CMakeLists.txt:66-70`). [邏輯衝突?] No (Stale-doc-only — §5 explicitly allows C++20+).
- **Hard rule 1: Player.h must not #include any concrete umbrella header** — decoupling rule.
  [是否實作?] Yes — `include/Player.h:3-9` includes only `Character.h`/gfx/std (verified). [邏輯衝突?] No.
- **Hard rule 2: Item/Umbrella files must not call raylib draw primitives — emit events via EventBus** — UI/Data separation.
  [是否實作?] Partial — items still avoid raylib directly but now go through `gfx/IRenderer` not RenderRequested events; `TransparentUmbrella::Draw` no longer publishes a render event (architecture re-pointed at MVC `View`). [邏輯衝突?] No (Stale-doc-only — supersession; MVC purity instead per CLAUDE.md §5).
- **Hard rule 3: No delete mid-iteration; mark isActive=false + end-of-frame sweep** — deferred deletion.
  [是否實作?] Yes — CLAUDE.md §5 codifies this; `isActive_` guards in `Cursed/ProfTrap/True/Fragile` (BUGLEDGER L2 hardened). [邏輯衝突?] No.
- **Out of scope for Day 1: Fragile/ProfTrap/Cursed umbrellas, NPC, SemesterStateMachine, real sprites** — Day 2-3 work.
  [是否實作?] Yes — all four classes shipped (`include/FragileUmbrella.h`, `include/CursedUmbrella.h`, `include/ProfessorTrapUmbrella.h`, `include/NPC.h`, `include/SemesterStateMachine.h`); real textures live (CHANGELOG Cycle 6 UI shell). [邏輯衝突?] No (Stale-doc-only — explicitly Day 2-3 trajectory).
- **Task 1: CMake hardening + doctest setup** — `-Wall -Wextra -Wpedantic`, FetchContent doctest, tests target.
  [是否實作?] Yes — `CMakeLists.txt:11,33-44,65-77`. [邏輯衝突?] No.
- **Task 2: GameObject abstract base (position, hitBox, isActive, collisionLayer; pure Update/Draw/Interact; CheckCollision)** — base class spec.
  [是否實作?] Yes — `include/GameObject.h` (66 lines) implements the contract (plus virtual `IsVendor`/`NpcId`/`BlocksMovement` added per BUGLEDGER I5). [邏輯衝突?] No.
- **Task 3: Character abstract (speed, direction, currentFrame, Move)** — intermediate class.
  [是否實作?] Yes — `include/Character.h` (38 lines). [邏輯衝突?] No.
- **Task 4: Player concrete (rainMeter, karma, hasUmbrella, HandleInput WASD, decreaseKarma, resetRainMeter)** — player class.
  [是否實作?] Yes — `include/Player.h` (151 lines, plus tint/persona/money/inventory shipped beyond plan); WASD input + karma + rain meter all present (CHANGELOG Cycle 7 R5 wired). [邏輯衝突?] No (Stale-doc-only — expanded API).
- **Task 5: EventBus (Singleton; Subscribe/Publish/Clear; EventType: RenderRequested, UmbrellaClaimed, KarmaChanged, ShowMessage)** — Observer.
  [是否實作?] Yes — `include/EventBus.h` (110 lines); BUGLEDGER B1/B2/H1 fixed includes/dangling-handlers/added `ScopedSubscribe` RAII. Event types extended (`PickupAcquired`, etc.). [邏輯衝突?] No.
- **Task 6: Item + TransparentUmbrella abstract (Draw emits RenderRequested via EventBus)** — abstract chain.
  [是否實作?] Partial — `include/Item.h`/`include/TransparentUmbrella.h` exist; Draw no longer publishes RenderRequested (MVC-pure via `IRenderer`). [邏輯衝突?] No (Stale-doc-only — architecture pivot).
- **Task 7: TrueUmbrella concrete (beClaimed sets hasUmbrella, publishes UmbrellaClaimed + ShowMessage)** — concrete leaf.
  [是否實作?] Yes — `src/TrueUmbrella.cpp` with §5 `isActive_` idempotency guard (BUGLEDGER L2). [邏輯衝突?] No.
- **Task 8: GameObjectFactory (enum {Player, TrueUmbrella}, static Create)** — Factory Method.
  [是否實作?] Yes — `include/GameObjectFactory.h:7-24` extended to 11 ObjectType variants (all umbrellas, HotPack/WaterproofSpray/EnergyDrink, Vendor, CashPickup×3). [邏輯衝突?] No (Stale-doc-only — extended).
- **Task 9: Wire main.cpp game loop (vector unique_ptr, factory populate, deferred-removal sweep, console-print EventBus handler)** — composition root.
  [是否實作?] Yes — `src/main.cpp` (120 lines) is the composition root (Title→Select→Playing screen-flow + Harness MaybeAttach/WireEvents/BeginFrame/EndFrame). Deferred sweep idiom preserved. [邏輯衝突?] No (Stale-doc-only — full game vs MVP stub).
- **Task 10: Verification gate — architecture-reviewer + gof-pattern-auditor + manual smoke + push** — process gates.
  [是否實作?] Yes — every cycle gates 0-warn + 289/289 tests + dialog_lint + ending byte-determinism (CHANGELOG Cycle 8 gate). [邏輯衝突?] No.
- **Self-Review Checklist (7 rubric items, 3 hard rules, signature consistency)** — completion criteria.
  [是否實作?] Yes — all 7 rubric satisfied; 3 hard rules enforced (CLAUDE.md §5). [邏輯衝突?] No.
- **Day 2-3 Roadmap (extra umbrellas, SemesterStateMachine, NPC, sprites, UML PNG)** — forward plan.
  [是否實作?] Yes — every listed item shipped across Cycles 1–8 (CHANGELOG). [邏輯衝突?] No.
- **Test count target: 4 cases (2 EventBus + 2 Factory)** — verification scope.
  [是否實作?] Yes — surpassed: 289/289 tests, 4254 assertions (Cycle 8 gate). [邏輯衝突?] No.

## Summary

- Total elements audited: **19**
- Yes: **14** · Partial: **3** · No: **0** · N/A: **0**
- Real conflicts (current-direction): **0**
- Stale-doc-only divergences: **8** (C++17→20, RenderRequested→IRenderer, MVP main.cpp stub→full composition root, color-block→real textures, scope expansion in Factory/Player/EventBus/Hard-rule-2 architecture pivot)
- Verdict: plan is **fully delivered and surpassed**; no contradictions with current direction. Treat as historical baseline per CLAUDE.md §2.
