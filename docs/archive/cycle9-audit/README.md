# Cycle 9 audits

Forward-looking audits produced during Cycle 9 (post-Cycle-8 audit
pass). Distinct from `docs/cycle8-audit/` which catalogued
implementation vs design drift; this directory holds new-issue
discovery.

- `accessibility-audit.md` — baseline WCAG 2.1 AA audit (2 BLOCKING,
  3 HIGH, 3 MEDIUM, 3 NA). Produced by the borrowed CCGS
  `accessibility-specialist` agent (after two failed attempts +
  one general-purpose retry — see CHANGELOG for the agent-
  definition adaptation story).
