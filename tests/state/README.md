# tests/state — semester state machine, interludes, ending gate

## Files

- `test_state_machine.cpp` — `SemesterStateMachine` transitions
- `test_interlude_exit.cpp` — exit state core
- `test_interlude_exit_feedback.cpp` — exit player feedback
- `test_interlude_exit_marker.cpp` — exit visual marker
- `test_ending_gate.cpp` — endings A/B/C decision

## Dependency direction

- Depends on: controller, dialog, entities, gfx, quest, ui, world
- Used by: nothing (test leaf)
