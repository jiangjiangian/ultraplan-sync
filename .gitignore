# Build artifacts
build/
*.o
*.obj
*.exe

# Secrets / API keys
.env
.env.*
*.key

# IDE
.vscode/
.idea/
*.swp

# macOS
.DS_Store

# Generated images output (Nano Banana default dir is ~/nanobanana-images, but if user redirects locally)
nanobanana-output/

# AI agent / automation artifacts — local development only, never push
# (Claude Code config, Codex CLI cache, agent manifests, implementation plans)
.claude/
.codex/
AGENTS.md
CLAUDE.md
docs/superpowers/
