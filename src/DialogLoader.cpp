#include "DialogLoader.h"

namespace nccu::dialog {

// Stub: real parser arrives in the next commit.
LoadedChapter LoadChapter(const std::string& /*path*/) {
    return LoadedChapter{};
}

}  // namespace nccu::dialog
